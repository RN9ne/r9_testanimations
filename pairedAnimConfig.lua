-- ================================
-- PAIRED ANIMATION CONFIGURATION
-- ================================
-- General configuration for paired animations (NPC + Player)

PairedAnimConfig = {
    -- NPC model used for paired animations
    PedModel = "a_m_m_business_01",
    
    -- Distance to spawn NPC in front of player
    SpawnDistance = 1.5,
    
    -- Default animation flags for playback
    DefaultAnimationFlags = 1,

    -- NPC auto-cleanup timeout in milliseconds (10 seconds)
    AutoCleanupTime = 20000
}
